{
  "name": "crav-stripe-webhooks",
  "version": "1.0.0",
  "description": "Stripe webhook handler for CR AudioViz AI payment automation",
  "main": "api/webhooks/stripe.js",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "stripe": "^14.10.0",
    "@supabase/supabase-js": "^2.39.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
